/**
 * Source for a component from anywhere
 * Check signature
 * Copy to local cache
 *
 * @author: Bernhard Lukassen
 */

import path                 from '/path';
import fs                   from '/fs';

export default class Source {

    constructor(uri) {
        Object.assign(this, { uri });
    }

    isValid() {

    }

    copy2cache(cache) {

    }

    resolve() {

    }
}
